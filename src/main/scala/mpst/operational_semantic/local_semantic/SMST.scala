/*
  Synchronous Multiparty Session Types - Andi Bejleri & Nobuko Yoshida
  1st iteration:
    - suppressing high order communication
    - suppressing multicasting
    - suppressing delegation
    - suppressing session initialisation
    - check structural congruence
*/

package mpst.operational_semantic.local_semantic

import mpst.syntax.Protocol
import mpst.syntax.Protocol._



object SMST:
  private def reduce(environment: Map[String,Protocol], local: Protocol): List[(Action,State)] =
    ???
  end reduce
end SMST